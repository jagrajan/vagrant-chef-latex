# Vagrant and Chef config for Latex

## Usage

```
vagrant up
vagrant ssh
```

This set up is only configured to install some latex packages. If you need all latex packages, run:
```
sudo apt-get install texlive-full
```
